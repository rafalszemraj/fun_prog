1. First class functions
1.1 Functions are callable

	var hi = function(name) {
	  return 'Hi ' + name;
	};

	var greeting = function(name) {
	  return hi(name);
	};

	greeting("John") -> "Hi John"

	//

	var greeting = hi;
	greeting("John") -> "Hi John"


	second example

	var getServerStuff = function(callback) {
		 return ajaxCall(function(json) {
		    return callback(json);
	 });
	};

2. Pure functions
2.1 cacheable (memoizable)
2.2 self documenting, easy to test (no mocks, stubs just input -> output )
2.3 no side efects..
2.4 .. so no race conditions, vergy good for parallel programing
2.5 no ugly suprises ->

	example with plus,doubles in here

3. Currying and partial applied functions
3.1 function compositions

	example

3.2. pointfree style - forget about your data for a while

	example (lodash vs ramda)

4. transactions exercise (live coding)
5. What next??
5.1 Monads
5.2 Transducers (reduce on steroids)
5.3 Category Theory

5.6 exercises
